// import { createHooks } from 'hookable'

// export interface MyHooks {
  // 'sse:event': (data: any) => any | void
// }

// export const myHooks = createHooks<MyHooks>() 

////myHooks.callHook("sse:event", { message: "Hello!" })

///import { createHooks } from 'hookable'

// Create a hookable instance
///export const myHooks = createHooks()

// Hook on 'hello'
//myHooks.hook('hello', () => { console.log('Hello World' )})

//myHooks.callHook("sse:event", { message: "Hello!" })

// Call 'hello' hook
//myHooks.callHook('hello')